<UserControl x:Class="QuizMaker.Views.StartPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizMaker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Width="300" VerticalAlignment="Center">
            <Button Command="{Binding CreateDatabaseCommand}" Height="40" Margin="0 0 0 10">Utwórz nową bazę quizów</Button>
            <Button Command="{Binding OpenDatabaseCommand}" Height="40">Otwórz bazę quizów z pliku .db</Button>
        </StackPanel>

    </Grid>
</UserControl>
